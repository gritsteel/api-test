# 지역별 누적 강수량 및 농업용수 저수율 — 데이터 설계

대상 화면: **지역별 강수량·저수율 조회 (정적 페이지)**
데이터 출처: 농림축산식품 공공데이터 포털 OpenAPI
`http://211.237.50.150:7080/openapi/{인증키}/xml/Grid_20250220000000000669_1/{시작행}/{종료행}`

---

## 0. 검증 상태

수집은 완료됐고, 아래 수치는 전량 `data.json` 을 실제로 집계한 결과다.

| 항목 | 값 |
|---|---|
| 총 건수 | **66,263** (API `totalCnt` 와 일치) |
| 수집 방식 | 1000행씩 67회 분할 요청 후 병합 |
| 결측·비수치 | **0건** (두 측정값 모두) |
| `ROW_NUM` | 1~66,263 연번, 누락 없음 |
| `(TOT_YM, AREA_SPR_CD)` 중복 | 0건 |

등급 표기는 이전과 동일하다. **A** = 데이터/코드로 확인됨, **B** = 근거는 있으나 미검증,
**C** = 이 데이터에 없음.

---

## 1. 원본 스키마 (7개 필드)

| 영문 키 | 예시 | 의미 | 단위 | 등급 |
|---|---|---|---|---|
| `ROW_NUM` | `1` | 행 번호 (전역 연번) | — | **A** |
| `TOT_YM` | `202607` | 집계 연월 | `YYYYMM` | **A** |
| `AREA_SPR_CD` | `108` | 지역 구분 코드 = **기상청 관측지점번호** | — | **A** |
| `RNFL_MSRVL` | `8.68` | 강수량 측정값 — **일평균** (2.2절) | mm/일 추정 | **B** |
| `STWTR_RTO_MSRVL` | `71.1` | 농업용수 저수율 측정값 | % 추정 | **B** |
| `STR_DT` | `2026-09-03` | 적재일자 (전 행 동일) | `YYYY-MM-DD` | **A** |
| `STR_USERID` | `ETL` | 적재 주체 (전 행 동일) | — | **A** |

단위는 응답에 없다. 저수율 값이 0~101.44 범위라 `%` 가 유력하나 활용가이드로 확정할 것.

### 1.1 `AREA_SPR_CD` 가 기상청 관측지점번호라는 근거

포털 응답에는 코드표가 없다. 아래 대조로 정체를 확정했다.

- 이 저장소 `weather.py:STATIONS` 의 8개 지점번호(서울 108 · 인천 112 · 부산 159 ·
  대구 143 · 광주 156 · 대전 133 · 울산 152 · 제주 184)가 **8/8 모두** 코드 집합에 존재
- 널리 알려진 ASOS 지점번호 표본 20개도 **20/20 존재**
- 코드 개수 746종, 범위 42~996 — 기상청 관측지점 규모와 부합

따라서 지역명 매핑은 기상청 지점번호 체계를 쓴다. 다만 **공식 코드표를 확보하지
못했으므로 전량 매핑은 불가**하다(4장).

---

## 2. 데이터 실측 프로파일

| 지표 | 값 |
|---|---|
| `TOT_YM` 종류 | **93개월** (`201901` ~ `202609`) |
| `AREA_SPR_CD` 종류 | **746종** |
| 연월×지역 완전조합 | 69,378 (실제 66,263 → **희소, 4.5% 결손**) |
| 월당 행 수 | 최근 6개월 736~742행 |
| `RNFL_MSRVL` | min 0 / 중앙값 2.38 / max 64.53 |
| `STWTR_RTO_MSRVL` | min 0 / 중앙값 **0** / max 101.44 |

### 2.1 저수율 0 은 전부 결측이다

원본은 자료가 없는 구간을 `null` 이 아니라 `0` 으로 채워 보낸다. 근거는 셋이다.

| 관측 | 수치 |
|---|---|
| `STWTR_RTO_MSRVL == 0` 인 행 | 57,033건 (86.1%) |
| 전 기간 0인 지역코드 | 249종 / 746 |
| 값이 존재하는 497곳의 **최초 비0 월** | **2025-02 에 484곳(97%)이 동시 시작** |
| 시작 이후 나타나는 0 | **전량 2025-10 한 달, 482곳 동시** |

전국 484곳의 저수율이 같은 달에 0에서 동시에 튀어오르고, 또 다른 한 달에만
482곳이 동시에 0이 되는 물리적 이유는 없다. 수집이 2025-02 에 시작됐고
2025-10 한 달이 결손인 것으로 읽는 것이 타당하다.

**설계 결정**

- 저수율 값 `0` 은 **전부 `null`(결측)로 변환**한다. 0을 값으로 그리지 않는다
- 유효 구간은 **2025-02 ~ 2026-09**, 그 중 **2025-10 은 결손**이다
- 저수율 값이 하나도 없는 지역(249곳)은 저수율 차트·타일을 표시하지 않는다
- 저수율 차트의 가로축은 값이 존재하는 구간으로 **자동 축소**한다.
  93개월 축에 19개월치를 그리면 대부분이 빈 공간이 된다
- 강수량 0(3.8%)은 정상값이므로 그대로 둔다. 강수량은 이 규칙의 대상이 아니다

### 2.2 `RNFL_MSRVL` 은 월 누적이 아니라 일평균이다

필드명은 "누적"이지만 값의 크기가 맞지 않는다. 달력 월별 전국 평균을 집계했다.

| 월 | 원값 평균 | × 해당월 일수 | 월 | 원값 평균 | × 해당월 일수 |
|---|---|---|---|---|---|
| 01 | 0.83 | 25.7 | 07 | **9.38** | **290.9** |
| 02 | 1.12 | 31.3 | 08 | 7.83 | 242.8 |
| 03 | 1.80 | 55.9 | 09 | 6.91 | 207.3 |
| 04 | 2.27 | 68.2 | 10 | 2.67 | 82.9 |
| 05 | 3.42 | 106.0 | 11 | 1.86 | 55.9 |
| 06 | 5.09 | 152.6 | 12 | 0.85 | 26.4 |

계절 형태(7월 최대, 1·12월 최소)는 한국 강수 분포와 일치한다. 그러나 **크기가
맞지 않는다.** 원값을 월 누적으로 읽으면 7월 전국 평균이 9.4mm 라는 뜻이 되어
실제와 한 자릿수 어긋난다. 일수를 곱한 291mm 는 한국 7월 강수량과 부합한다.

**설계 결정**: 단위를 `mm/일(추정)`로 표기하고 "누적"이라는 말을 화면에서 쓰지
않는다. 월 환산값(원값 × 일수)은 파생 지표라 이번 범위에 넣지 않는다 — 단위
자체가 아직 추정이므로 곱셈으로 오차를 키우지 않는다. 공식 정의 확인 후 결정한다(6장).

---

## 3. 화면 구성

```
┌─────────────────────────────────────────┐
│ [A] 필터   지역 선택 · 기간 선택           │
├─────────────────────────────────────────┤
│ [B] 요약   최신 강수량 · 최신 저수율        │
│            관측 개월 수                    │
├─────────────────────────────────────────┤
│ [C] 차트1  월별 누적 강수량 추이            │
├─────────────────────────────────────────┤
│ [D] 차트2  월별 저수율 추이                │
│            (미제공 지역이면 안내로 대체)     │
├─────────────────────────────────────────┤
│ [E] 표     월별 원자료 (접이식)             │
└─────────────────────────────────────────┘
```

**차트 1과 2는 분리한다.** 강수량(mm)과 저수율(%)은 척도가 달라 한 그래프에 두 축을
두면 안 된다. 각각 단일 축 차트로 그린다.

---

### 3.1 화면 A — 조회 도구의 레이아웃

값을 찾는 도구이므로 **한 화면에 다 들어와야 한다.** 스크롤해서 찾는 도구는 도구가
아니다. 뷰포트 높이에 고정하고 세로 스크롤을 없앤다.

```
┌─────────────────────────────────────────┬──────────┐
│ 상단 바 — 로고 · 개요로 돌아가기           │          │
├─────────────────────────────────────────┤  지역 검색 │
│ 제목 · 메타                               │  지역     │
│ ┌────┬────┬────┬────┐                   │  시작     │
│ │타일│타일│타일│타일│                   │  종료     │
│ └────┴────┴────┴────┘                   │ ───────  │
│ ┌──────────────┬──────────────┐   [표] ←┤  표 보기  │
│ │  강수량 차트  │  저수율 차트  │   서랍  │          │
│ └──────────────┴──────────────┘         │          │
└─────────────────────────────────────────┴──────────┘
```

**규칙**

- `html`·`body` 를 `height:100%`, `overflow:hidden` 으로 두고 내부를 flex 로 채운다.
  `100vh` 를 쓰지 않는다 — 모바일 주소창 때문에 어긋난다
- 필터 4개(지역 검색·지역·시작·종료)는 **오른쪽 열에 세로 일렬**로 둔다.
  값을 바꾸는 손이 한 곳에 모인다
- 두 차트는 **가로로 나란히** 둔다. 저수율이 미제공인 지점에서는 강수량 차트가
  가로 전체를 쓴다
- 표는 오른쪽 탭을 눌러 **서랍처럼 펼친다.** 표 안에서만 세로로 스크롤한다
- 차트 SVG 는 컨테이너의 실제 픽셀 크기로 다시 그린다. `viewBox` 를 늘려 늘어나게
  두면 선 두께와 글자가 함께 찌그러진다. 창 크기가 바뀌면 다시 그린다
- **좁은 화면(960px 미만)에서는 이 고정 레이아웃을 풀고** 세로 스크롤로 되돌린다.
  높이가 모자란 화면에 억지로 밀어 넣으면 아무것도 안 보인다

### 3.2 화면 B — 랜딩

화면 A(`explore.html`)는 값을 **찾는** 도구다. 화면 B(`index.html`)는 이 데이터가
무엇인지 **읽게** 하는 진입 페이지다. 목적이 달라 레지스터를 분리한다.

레퍼런스는 기업 소개 사이트의 구성을 따른다 — 밝고 여백이 큰 바탕, 풀블리드
히어로, 그 위에 떠 있는 알약형 내비게이션, 중간에 풀블리드 다크 인용 밴드,
다단 링크 푸터, 우하단 맨위로 버튼.

**사진을 한 장도 쓰지 않는다.** 인물 클로즈업과 항공기 소재는 제외하며, 레퍼런스에서
사진이 놓이던 자리는 전부 **데이터로 그린 대형 그래픽**이 대신한다. 장식용 이미지가
0개라는 뜻이다 — 화면에 크게 걸리는 모든 형태는 66,263건에서 나온 실제 값이다.

| 레퍼런스의 자리 | 이 페이지에서 |
|---|---|
| 히어로 풍경 사진 | 달력 월별 전국 평균 강수량 곡선을 지평선처럼 가로로 채운 그래픽 |
| 다크 밴드의 인물 사진 | 월별 저수율 보유 지점 수 — 73개월 0, 2025-02 급등, 2025-10 결손 |
| 인용문 | 2.1절의 판단을 한 문장으로: 원본은 없는 값을 0으로 채워 보낸다 |

**섹션 구성**

| # | 섹션 | 쓰는 값 |
|---|---|---|
| 1 | 히어로 — 교체되는 문장 3개 + 지평선 그래픽 | 월별 평균 `RNFL_MSRVL` |
| 2 | 숫자 — 기록의 규모 | `meta.total`, 지점 수, 개월 수, 결측률 |
| 3 | 비의 리듬 | 달력 월별 전국 평균, 일수 환산 대조 |
| 4 | 다크 밴드 — 0은 값이 아니다 | 월별 저수율 보유 지점 수 |
| 5 | 지점으로 들어가기 | 화면 A 링크 |
| 6 | 푸터 | 출처, 한계, 갱신일 |

**로고.** 브랜드명은 **강수**다. 문서 제목과 내비·푸터 모두 이 이름을 쓴다. 물방울 안에 비스듬한 청록 띠를 넣은 심볼이며,
화면 B의 물방울 픽토그램과 같은 형태 언어를 쓴다.

- 밝은 바탕: 가로형(심볼 + 워드마크)
- **어두운 바탕: 심볼만 쓴다.** 워드마크 잉크가 `#201e1d` 라 어두운 면에서 사라진다
- 라이트/다크를 오가는 화면 A 에서도 심볼만 쓴다

**타이포그래피.** 두 벌 — `IBM Plex Sans KR`(표제 600 / 본문 300·400)과
`IBM Plex Mono`(수치). 수치는 항상 고정폭으로 둔다.

**색.** 로고의 청록을 기준으로 한 단일 색상 램프를 쓴다. 두 화면이 같은 팔레트를
공유하며 **라이트 단일**이다 — 자동 반전하지 않고 모든 색을 명시적으로 칠한다.

| 역할 | 값 | 바탕 위 대비 |
|---|---|---|
| 바탕 / 표면 | `#f6f9fa` / `#ffffff` | — |
| 본문 / 보조 / 약한 글자 | `#0e1a1f` / `#44565e` / `#5b6a71` | 16.73 / 7.25 / 5.30 |
| 브랜드(링크·포커스) | `#00728f` | 5.22 |
| 강수 램프 t1~t6 | `#a8ebff` `#18cbff` `#00a6d6` `#007da2` `#00607d` `#00475b` | — |
| 저수율 마크 / 글자 | `#d9632c` / `#b34a1a` | 3.44 / 5.08 |
| 다크 밴드 바탕 / 글자 | `#0a303e` / `#f4f8fa` | 13.06 |

**명도에는 쓸 수 없는 구간이 있다.** 흰 글자는 상대휘도 0.183 이하에서만, 잉크
글자(`#0a303e`)는 0.289 이상에서만 4.5:1 을 넘는다. 그 사이 구간에 놓인 색은 어떤
글자도 얹을 수 없다. **로고 원색 `#0088b0` 의 휘도가 0.207 로 정확히 그 안에 있어서**
램프에 그대로 쓰지 않고, 색상각은 유지한 채 구간 밖으로 밟은 값을 쓴다.

물방울처럼 연한 단계(t1~t3)는 면만으로는 바탕과 구분되지 않으므로
**t5(`#00607d`) 2px 윤곽선을 모든 물방울에 넣는다.** 형태가 색에 의존하지 않게 한다.

---

## 4. 화면 ↔ 데이터 대응표

| 화면 항목 | 영문 키 | 변환 | 등급 |
|---|---|---|---|
| 지역 선택 값 | `AREA_SPR_CD` | 코드 그대로 | **A** |
| 지역 표시명 | `AREA_SPR_CD` 매핑 | 검증된 8곳만 실명, 나머지는 `지점 {코드}` | **부분** |
| 기간 축 | `TOT_YM` | `YYYYMM` → `YYYY-MM` | **A** |
| 강수량 값 | `RNFL_MSRVL` | `parseFloat`. 단위 `mm/일` 표기 (2.2절) | **A** |
| 저수율 값 | `STWTR_RTO_MSRVL` | `parseFloat` 후 `0 → null` (2.1절) | **A** |
| 최신 강수량 타일 | `RNFL_MSRVL` | 최신 `TOT_YM` 의 값 | **A** |
| 최신 저수율 타일 | `STWTR_RTO_MSRVL` | 동일. 미제공 지역이면 숨김 | **A** |
| 관측 개월 수 | 행 수 | 해당 지역 행 개수 | **A** |
| 적재일 표기 | `STR_DT` | 전 행 동일값 1개 | **A** |

### 4.1 지역명 매핑 — 이 설계의 유일한 공백

| 구분 | 개수 | 근거 |
|---|---|---|
| 실명 표시 | 8 | 저장소 `STATIONS` 로 교차검증됨 (**A**) |
| 코드 표시 | 738 | 공식 코드표 미확보 |

`지점 {코드}` 로 표시하고 실명을 지어내지 않는다. 기상청 지점정보 또는 포털
활용가이드의 코드표를 확보하면 매핑 테이블만 교체해 738곳이 한 번에 실명이 된다.

### 4.2 이 데이터에 없는 것 (**C**)

기온, 습도, 바람, 기압, 날씨 상태, 일별·시간별 값. 이 데이터셋은 **월 단위 집계**이며
실시간 관측이 아니다. 해당 항목을 화면에 넣지 않는다.

---

## 5. 데이터 설계

### 5.1 파일 구성

| 경로 | 내용 | 크기 |
|---|---|---|
| `data.json` | 원본 전량 66,263건 · 7필드. 압축 직렬화(BOM 없음) | 약 8MB |
| `build/data.json` | 화면 전용 축약본 | 약 1MB |
| `build/index.html` | 화면 B — 에디토리얼 랜딩 (3.2) |  |
| `build/explore.html` | 화면 A — 조회 도구 (3.1). 화면 B 와 같은 팔레트·타이포 |  |
| `build/logo/*.svg` | 로고 3종 — 가로형·세로형·심볼 | 33KB |

`build/data.json` 축약 규칙 — **값을 버리지 않고 표현만 바꾼다.**

- 전 행 동일한 `STR_USERID` 는 메타로 이동, `STR_DT` 도 메타로 이동
- `ROW_NUM` 은 배열 순서로 대체
- 지역별로 묶고 93개월 축에 정렬, 해당 월 자료가 없으면 `null`
- 수치는 문자열이 아닌 숫자로

```
{
  "meta":   { "total": 66263, "loadedAt": "2026-09-03", "source": "ETL",
              "months": ["201901", ...],        // 93개
              "units":  { "rainfall": "mm(추정)", "storage": "%(추정)" } },
  "areas":  [ { "code": "108", "name": "서울",
                "rainfall": [8.68, null, ...],  // months 와 같은 길이
                "storage":  [71.1, null, ...],
                "hasStorage": true } ]
}
```

`hasStorage` 가 2.1절 규칙의 구현체다. 0을 `null` 로 바꾼 뒤 남는 값이 하나도
없으면 `false` 로 굳혀 둔다. 화면은 이 값만 보고 저수율 영역 표시 여부를 정한다.

### 5.2 로딩

- 페이지는 `fetch("data.json")` 으로 **축약본만** 읽는다. 원본은 읽지 않는다
- 로딩 중 스켈레톤, 실패 시 오류 문구를 표시한다
- 지역 전환은 이미 받은 메모리 데이터로 처리한다 (재요청 없음)

### 5.3 시각화 규칙

- **이중 축 금지.** 강수량·저수율은 별개 차트
- 계열이 1개인 차트에는 범례를 두지 않는다 (제목이 계열명 역할)
- 강수량 = 기본 순차 색(파랑), 저수율 = 두 번째 슬롯(주황). 검증된 팔레트 값 사용
- 라이트/다크 각각의 값을 별도로 지정한다 (자동 반전 금지)
- 호버 시 십자선과 값 표시
- 표 보기를 제공해 색에만 의존하지 않게 한다

### 5.4 배포

- 배포 대상은 `build/` 폴더뿐이다
- **정적 호스트의 자산 루트는 `build/` 자신이어야 한다.** `build` 폴더를 통째로
  올리면 경로가 `/build/index.html` 이 되어 `/explore.html` 이 404 가 되고,
  SPA 폴백이 켜져 있으면 404 대신 랜딩이 다시 뜬다 — 링크를 눌러도 제자리인 것처럼 보인다
- `.html` 확장자를 붙인 경로가 그대로 서빙돼야 한다. 확장자를 떼는 자동 변환이
  켜져 있으면 페이지 간 링크가 리다이렉트를 한 번 거친다
- 정적 파일만 두며 빌드 단계나 서버 런타임이 없다

---

## 6. 미해결 항목

1. **지역코드 738곳의 실명** — 코드표 확보 시 매핑 테이블만 교체
2. **두 측정값의 단위** — `mm/일`, `%` 를 활용가이드로 확정
3. **저수율 수집 시작이 2025-02 인지** — 제공기관 확인. 맞다면 화면에
   "2025-02 부터 제공" 안내를 상시 노출한다
4. **2025-10 결손 사유** — 일시 중단인지 누락인지
5. **`RNFL_MSRVL` 의 공식 정의** — 일평균이 확정되면 월 환산값(원값 × 일수)을
   보조 지표로 추가할지 결정한다
