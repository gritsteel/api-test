# 지역별 누적 강수량 및 농업용수 저수율 — 데이터 설계

대상 화면: **지역별 강수량·저수율 조회 (정적 페이지)**
데이터 출처: 농림축산식품 공공데이터 포털 OpenAPI
`http://211.237.50.150:7080/openapi/{인증키}/xml/Grid_20250220000000000669_1/{시작행}/{종료행}`

---

## 0. 검증 상태

수집은 완료됐고, 아래 수치는 전량 `data.json` 을 실제로 집계한 결과다.

| 항목 | 값 |
|---|---|
| 총 건수 | **66,263** (API `totalCnt` 와 일치) |
| 수집 방식 | 1000행씩 67회 분할 요청 후 병합 |
| 결측·비수치 | **0건** (두 측정값 모두) |
| `ROW_NUM` | 1~66,263 연번, 누락 없음 |
| `(TOT_YM, AREA_SPR_CD)` 중복 | 0건 |

등급 표기는 이전과 동일하다. **A** = 데이터/코드로 확인됨, **B** = 근거는 있으나 미검증,
**C** = 이 데이터에 없음.

---

## 1. 원본 스키마 (7개 필드)

| 영문 키 | 예시 | 의미 | 단위 | 등급 |
|---|---|---|---|---|
| `ROW_NUM` | `1` | 행 번호 (전역 연번) | — | **A** |
| `TOT_YM` | `202607` | 집계 연월 | `YYYYMM` | **A** |
| `AREA_SPR_CD` | `108` | 지역 구분 코드 = **기상청 관측지점번호** | — | **A** |
| `RNFL_MSRVL` | `8.68` | 누적 강수량 측정값 | mm 추정 | **B** |
| `STWTR_RTO_MSRVL` | `71.1` | 농업용수 저수율 측정값 | % 추정 | **B** |
| `STR_DT` | `2026-09-03` | 적재일자 (전 행 동일) | `YYYY-MM-DD` | **A** |
| `STR_USERID` | `ETL` | 적재 주체 (전 행 동일) | — | **A** |

단위는 응답에 없다. 저수율 값이 0~101.44 범위라 `%` 가 유력하나 활용가이드로 확정할 것.

### 1.1 `AREA_SPR_CD` 가 기상청 관측지점번호라는 근거

포털 응답에는 코드표가 없다. 아래 대조로 정체를 확정했다.

- 이 저장소 `weather.py:STATIONS` 의 8개 지점번호(서울 108 · 인천 112 · 부산 159 ·
  대구 143 · 광주 156 · 대전 133 · 울산 152 · 제주 184)가 **8/8 모두** 코드 집합에 존재
- 널리 알려진 ASOS 지점번호 표본 20개도 **20/20 존재**
- 코드 개수 746종, 범위 42~996 — 기상청 관측지점 규모와 부합

따라서 지역명 매핑은 기상청 지점번호 체계를 쓴다. 다만 **공식 코드표를 확보하지
못했으므로 전량 매핑은 불가**하다(4장).

---

## 2. 데이터 실측 프로파일

| 지표 | 값 |
|---|---|
| `TOT_YM` 종류 | **93개월** (`201901` ~ `202609`) |
| `AREA_SPR_CD` 종류 | **746종** |
| 연월×지역 완전조합 | 69,378 (실제 66,263 → **희소, 4.5% 결손**) |
| 월당 행 수 | 최근 6개월 736~742행 |
| `RNFL_MSRVL` | min 0 / 중앙값 2.38 / max 64.53 |
| `STWTR_RTO_MSRVL` | min 0 / 중앙값 **0** / max 101.44 |

### 2.1 반드시 처리해야 할 특성 — 저수율 0

| 사실 | 수치 |
|---|---|
| `STWTR_RTO_MSRVL == 0` 인 행 | **57,033건 (86.1%)** |
| 저수율이 **항상 0**인 지역코드 | **249종 / 746** |
| 저수율이 한 번도 0이 아닌 지역코드 | 5종 |

전 지역 249곳이 모든 기간 0이라는 것은 "저수지가 말랐다"가 아니라 **해당 지점에
농업용수 저수지가 없거나 측정 대상이 아니다**로 읽는 것이 타당하다.

**설계 결정**: 저수율은 0을 그대로 그리지 않는다.

- 한 지역의 저수율이 전 기간 0이면 → 저수율 차트·타일을 **표시하지 않고**
  "저수율 미제공 지역"으로 안내
- 그 외 지역의 개별 0 값은 → 값으로 인정해 표시
- 강수량 0(3.8%)은 정상값이므로 그대로 표시

이 규칙 없이 그리면 화면 대부분이 바닥에 붙은 직선이 되어 정보가 없다.

---

## 3. 화면 구성

```
┌─────────────────────────────────────────┐
│ [A] 필터   지역 선택 · 기간 선택           │
├─────────────────────────────────────────┤
│ [B] 요약   최신 강수량 · 최신 저수율        │
│            관측 개월 수                    │
├─────────────────────────────────────────┤
│ [C] 차트1  월별 누적 강수량 추이            │
├─────────────────────────────────────────┤
│ [D] 차트2  월별 저수율 추이                │
│            (미제공 지역이면 안내로 대체)     │
├─────────────────────────────────────────┤
│ [E] 표     월별 원자료 (접이식)             │
└─────────────────────────────────────────┘
```

**차트 1과 2는 분리한다.** 강수량(mm)과 저수율(%)은 척도가 달라 한 그래프에 두 축을
두면 안 된다. 각각 단일 축 차트로 그린다.

---

## 4. 화면 ↔ 데이터 대응표

| 화면 항목 | 영문 키 | 변환 | 등급 |
|---|---|---|---|
| 지역 선택 값 | `AREA_SPR_CD` | 코드 그대로 | **A** |
| 지역 표시명 | `AREA_SPR_CD` 매핑 | 검증된 8곳만 실명, 나머지는 `지점 {코드}` | **부분** |
| 기간 축 | `TOT_YM` | `YYYYMM` → `YYYY-MM` | **A** |
| 강수량 값 | `RNFL_MSRVL` | `parseFloat` | **A** |
| 저수율 값 | `STWTR_RTO_MSRVL` | `parseFloat`, 2.1절 규칙 적용 | **A** |
| 최신 강수량 타일 | `RNFL_MSRVL` | 최신 `TOT_YM` 의 값 | **A** |
| 최신 저수율 타일 | `STWTR_RTO_MSRVL` | 동일. 미제공 지역이면 숨김 | **A** |
| 관측 개월 수 | 행 수 | 해당 지역 행 개수 | **A** |
| 적재일 표기 | `STR_DT` | 전 행 동일값 1개 | **A** |

### 4.1 지역명 매핑 — 이 설계의 유일한 공백

| 구분 | 개수 | 근거 |
|---|---|---|
| 실명 표시 | 8 | 저장소 `STATIONS` 로 교차검증됨 (**A**) |
| 코드 표시 | 738 | 공식 코드표 미확보 |

`지점 {코드}` 로 표시하고 실명을 지어내지 않는다. 기상청 지점정보 또는 포털
활용가이드의 코드표를 확보하면 매핑 테이블만 교체해 738곳이 한 번에 실명이 된다.

### 4.2 이 데이터에 없는 것 (**C**)

기온, 습도, 바람, 기압, 날씨 상태, 일별·시간별 값. 이 데이터셋은 **월 단위 집계**이며
실시간 관측이 아니다. 해당 항목을 화면에 넣지 않는다.

---

## 5. 데이터 설계

### 5.1 파일 구성

| 경로 | 내용 | 크기 |
|---|---|---|
| `data.json` | 원본 전량 66,263건 · 7필드. 압축 직렬화(BOM 없음) | 약 8MB |
| `build/data.json` | 화면 전용 축약본 | 약 1MB |
| `build/index.html` | 정적 페이지 1개 |  |

`build/data.json` 축약 규칙 — **값을 버리지 않고 표현만 바꾼다.**

- 전 행 동일한 `STR_USERID` 는 메타로 이동, `STR_DT` 도 메타로 이동
- `ROW_NUM` 은 배열 순서로 대체
- 지역별로 묶고 93개월 축에 정렬, 해당 월 자료가 없으면 `null`
- 수치는 문자열이 아닌 숫자로

```
{
  "meta":   { "total": 66263, "loadedAt": "2026-09-03", "source": "ETL",
              "months": ["201901", ...],        // 93개
              "units":  { "rainfall": "mm(추정)", "storage": "%(추정)" } },
  "areas":  [ { "code": "108", "name": "서울",
                "rainfall": [8.68, null, ...],  // months 와 같은 길이
                "storage":  [71.1, null, ...],
                "hasStorage": true } ]
}
```

`hasStorage` 가 2.1절 규칙의 구현체다. 전 기간 저수율이 0이면 `false` 로 굳혀 둔다.

### 5.2 로딩

- 페이지는 `fetch("data.json")` 으로 **축약본만** 읽는다. 원본은 읽지 않는다
- 로딩 중 스켈레톤, 실패 시 오류 문구를 표시한다
- 지역 전환은 이미 받은 메모리 데이터로 처리한다 (재요청 없음)

### 5.3 시각화 규칙

- **이중 축 금지.** 강수량·저수율은 별개 차트
- 계열이 1개인 차트에는 범례를 두지 않는다 (제목이 계열명 역할)
- 강수량 = 기본 순차 색(파랑), 저수율 = 두 번째 슬롯(주황). 검증된 팔레트 값 사용
- 라이트/다크 각각의 값을 별도로 지정한다 (자동 반전 금지)
- 호버 시 십자선과 값 표시
- 표 보기를 제공해 색에만 의존하지 않게 한다

### 5.4 배포

- 배포 대상은 `build/` 폴더뿐이다
- 정적 파일만 두며 빌드 단계나 서버 런타임이 없다

---

## 6. 미해결 항목

1. **지역코드 738곳의 실명** — 코드표 확보 시 매핑 테이블만 교체
2. **두 측정값의 단위** — 활용가이드로 `mm` / `%` 확정
3. **저수율 0의 공식 의미** — "미측정"인지 "실제 0"인지 제공기관 확인
4. **`RNFL_MSRVL` 이 월 누적인지 월평균인지** — 중앙값 2.38이 월 누적 강수량으로는
   작다. 일평균일 가능성이 있어 정의 확인 필요
