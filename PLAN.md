# 지역 날씨 화면 — 데이터 설계 및 API 대응표

대상 화면: **지역 날씨** (단일 지역의 현재 실황)
대상 API: `https://apihub.kma.go.kr/api/typ01/cgi-bin/url/nph-aws2_min`
(기상청 API Hub — 지상관측 AWS 매분자료)

---

## 0. 검증 상태 고지

이 문서를 작성한 환경에서는 **API 실호출이 불가능했다.**

| 시도 | 결과 |
|---|---|
| `curl https://apihub.kma.go.kr/...` | `CONNECT tunnel failed, response 403` |
| HTTP 페치 (동일 주소) | `EGRESS_BLOCKED` |
| `data.kma.go.kr` 문서 페이지 | `EGRESS_BLOCKED` |
| 환경변수 `KMA_APIHUB_KEY` | 미설정 |
| authKey 를 직접 주입해 재시도 | **동일하게 403** — 인증 문제가 아님 |

네트워크 이그레스 정책이 `apihub.kma.go.kr` 도메인을 차단했다. 유효한 authKey 를
주입해 재시도해도 결과는 같았다 — 프록시가 TLS CONNECT 터널 수립 단계에서
`403` 으로 거부하므로 요청이 기상청 서버에 **도달조차 하지 않는다.** 인증 문제가
아니라 호스트 허용 문제이며, 프록시 운영 문서는 이 경우 우회를 금지하고 있다.

따라서 아래 응답 스키마는 **실응답이 아니라 이 저장소의 파서 코드를 출처로 한다.**

각 항목에 아래 3등급 중 하나를 표기했다. 추측을 사실로 올리지 않기 위한 장치다.

| 등급 | 의미 |
|---|---|
| **A — 코드검증** | `weather.py` 가 해당 필드를 읽어 특정 의미의 값으로 변환한다. 필드 위치와 의미가 모두 코드로 확인된다. |
| **B — 위치확정/의미추론** | `_AWS_FIELDS` 에 위치가 고정돼 있으나 코드가 소비하지 않는다. 의미는 필드명 규칙에서 추론한 것이며 **실응답 대조가 필요하다.** |
| **C — 부재** | 이 API 응답에 해당 값이 없다. 확인된 부재다. |

**착수 전 필수 작업**: 네트워크가 열린 환경에서 아래를 1회 실행해 B등급을 A로 승격시킬 것.

```bash
curl "https://apihub.kma.go.kr/api/typ01/cgi-bin/url/nph-aws2_min?tm2=YYYYMMDDHHMM&stn=108&disp=0&help=1&authKey=$KMA_APIHUB_KEY"
```

`help=1` 이 응답 선두에 `#` 주석으로 항목 범례를 붙여준다. 그 범례가 이 문서의
B등급 행과 헤더 표기(대소문자·하이픈)를 확정하는 근거가 된다.

---

## 1. 응답 형식

- **Content-Type: 평문 텍스트.** JSON이 아니다.
- 한 줄이 한 관측 레코드이며 **공백으로 구분된 18개 컬럼**이다.
- `#` 로 시작하는 줄은 주석/범례이므로 건너뛴다.
- 인증 실패·활용신청 누락 시에는 예외적으로 **JSON 오류 객체**가 온다.
  → 본문이 `{` 로 시작하면 오류로 분기해야 한다.
- 결측/에러값은 **-50 이하의 음수 코드**로 온다 (주로 `-99.9`).

### 1.1 응답 컬럼 전량 (18개)

인덱스는 공백 분리 후 0-based 위치다. 영문 키는 이 저장소 파서의 정식 키다.

| # | 영문 키 | 추정 헤더 표기 | 의미 | 단위 | 등급 |
|---|---|---|---|---|---|
| 0 | `tm` | `TM` | 관측 시각 (`YYYYMMDDHHMM`) | KST | **A** |
| 1 | `stn` | `STN` | 관측지점번호 | — | **A** |
| 2 | `wd1` | `WD1` | 1분 평균 풍향 | deg (0–360) | **A** |
| 3 | `ws1` | `WS1` | 1분 평균 풍속 | m/s | **A** |
| 4 | `wds` | `WDS` | 순간 풍향 | deg | **B** |
| 5 | `wss` | `WSS` | 순간 풍속 (돌풍) | m/s | **B** |
| 6 | `wd10` | `WD10` | 10분 평균 풍향 | deg | **B** |
| 7 | `ws10` | `WS10` | 10분 평균 풍속 | m/s | **B** |
| 8 | `ta` | `TA` | 기온 | ℃ | **A** |
| 9 | `re` | `RE` | 강수 감지 (≥1 이면 감지) | 플래그 | **A** |
| 10 | `rn15m` | `RN-15m` | 15분 누적 강수량 | mm | **B** |
| 11 | `rn60m` | `RN-60m` | 60분 누적 강수량 | mm | **A** |
| 12 | `rn12h` | `RN-12H` | 12시간 누적 강수량 | mm | **B** |
| 13 | `rnday` | `RN-DAY` | 일 누적 강수량 | mm | **B** |
| 14 | `hm` | `HM` | 상대습도 | % | **A** |
| 15 | `pa` | `PA` | 현지 기압 | hPa | **A** |
| 16 | `ps` | `PS` | 해면 기압 | hPa | **B** |
| 17 | `td` | `TD` | 이슬점 온도 | ℃ | **A** |

> 헤더 표기 열은 기상청 관례에 따른 **추정**이다. `help=1` 범례로 확정할 것.
> 파싱은 헤더 문자열이 아니라 **컬럼 인덱스** 기준이므로 표기 차이가 동작에
> 영향을 주지는 않는다.

### 1.2 요청 파라미터

| 영문 키 | 값 | 필수 | 설명 |
|---|---|---|---|
| `authKey` | 발급 authKey | ✅ | 환경변수 `KMA_APIHUB_KEY` 에서 주입 |
| `stn` | 정수 지점번호 | ✅ | 위·경도 불가 |
| `tm2` | `YYYYMMDDHHMM` | ✅ | 조회 종료 시각 (KST) |
| `disp` | `0` | — | 표출 형식 |
| `help` | `1` | — | 항목 범례 포함 |

---

## 2. 화면 구성 정의

"지역 날씨" 한 줄만으로는 항목이 특정되지 않으므로, 일반적인 지역 날씨 화면을
4개 블록으로 정의하고 이를 대응표의 기준으로 삼는다. **실제 디자인과 다르면
이 절만 고쳐서 3장 표를 재매핑하면 된다.**

```
┌──────────────────────────────────────┐
│ [A] 헤더      지역명 · 관측시각        │
├──────────────────────────────────────┤
│ [B] 대표      아이콘 · 현재기온        │
│               날씨상태 · 체감온도       │
│               최고/최저                │
├──────────────────────────────────────┤
│ [C] 상세      습도 · 바람 · 강수        │
│               기압 · 이슬점             │
├──────────────────────────────────────┤
│ [D] 보조      시간별예보 · 주간예보     │
│               미세먼지 · 자외선 · 일출몰 │
│               기상특보 배너             │
└──────────────────────────────────────┘
```

---

## 3. 화면 ↔ API 대응표

**단일 API(`nph-aws2_min`) 기준.** 영문 키는 1.1절 파서 키다.

### [A] 헤더

| 화면 항목 | 영문 키 | # | 변환 | 등급 |
|---|---|---|---|---|
| 지역명 | — (`stn` 역매핑) | 1 | **API는 지점명을 주지 않는다.** `stn` → 지역명은 클라이언트 테이블(`STATIONS`)이 담당 | **C** |
| 관측 시각 | `tm` | 0 | `strptime("%Y%m%d%H%M")` + `tzinfo=KST` → ISO8601 | **A** |
| 지점번호 (디버그) | `stn` | 1 | 문자열 그대로 | **A** |

### [B] 대표

| 화면 항목 | 영문 키 | # | 변환 | 등급 |
|---|---|---|---|---|
| 현재 기온 | `ta` | 8 | `float`, ≤ -50 → `null`. 표시 `{:g}℃` | **A** |
| 강수 유무 | `re` | 9 | `≥1` → 있음 / `<1` → 없음 / `null` → 알 수 없음 | **A** |
| 날씨 상태 (맑음/구름/흐림) | **없음** | — | AWS에 운량·일사 항목 없음 → 6장 대안 | **C** |
| 날씨 아이콘 | **없음** | — | 상태값 부재로 직접 결정 불가 → 6장 대안 | **C** |
| 체감온도 | **없음** | — | `ta` + `ws1` + `hm` 으로 **파생 계산** → 6장 | **C** |
| 최고 / 최저 기온 | **없음** | — | 매분 스냅샷이라 일 통계 없음 → 6장 | **C** |

### [C] 상세

| 화면 항목 | 영문 키 | # | 변환 | 등급 |
|---|---|---|---|---|
| 습도 | `hm` | 14 | `float`, ≤ -50 → `null`. `{:g}%` | **A** |
| 풍속 | `ws1` | 3 | `float` m/s. `{:g}m/s` | **A** |
| 풍향 (각도) | `wd1` | 2 | `float` deg | **A** |
| 풍향 (16방위) | `wd1` 파생 | 2 | `COMPASS_16[int((deg+11.25)%360//22.5)]` | **A** |
| 돌풍 (순간최대풍속) | `wss` | 5 | `float` m/s — **실응답 확인 후 사용** | **B** |
| 돌풍 풍향 | `wds` | 4 | `float` deg — 동일 | **B** |
| 10분 평균 풍속 | `ws10` | 7 | `float` m/s — 동일 | **B** |
| 10분 평균 풍향 | `wd10` | 6 | `float` deg — 동일 | **B** |
| 강수량 (1시간) | `rn60m` | 11 | `float` mm. `0`/`null` 이면 행 숨김 | **A** |
| 강수량 (15분) | `rn15m` | 10 | `float` mm — **실응답 확인 후 사용** | **B** |
| 강수량 (12시간) | `rn12h` | 12 | `float` mm — 동일 | **B** |
| 강수량 (일누적) | `rnday` | 13 | `float` mm — 동일 | **B** |
| 기압 (현지) | `pa` | 15 | `float` hPa | **A** |
| 기압 (해면) | `ps` | 16 | `float` hPa — **실응답 확인 후 사용** | **B** |
| 이슬점 | `td` | 17 | `float` ℃ | **A** |
| 강수 형태 (비/눈/진눈깨비) | **없음** | — | `re` 는 감지 여부뿐 → 6장 | **C** |

### [D] 보조

| 화면 항목 | 영문 키 | 등급 | 비고 |
|---|---|---|---|
| 시간별 예보 | **없음** | **C** | 이 API는 관측 전용, 예보 없음 |
| 주간 예보 | **없음** | **C** | 동일 |
| 강수확률 (POP) | **없음** | **C** | 예보 영역 |
| 미세먼지 / 초미세먼지 | **없음** | **C** | 기상청 소관 아님 |
| 자외선 지수 | **없음** | **C** | 생활기상지수 별도 서비스 |
| 일출 / 일몰 | **없음** | **C** | 천문 정보, 기상청 소관 아님 |
| 기상특보 배너 | **없음** | **C** | 단, 본 저장소에 이미 구현돼 있음 (6장) |

---

## 4. 대응 요약

| 구분 | 개수 | 비중 |
|---|---|---|
| **A — 즉시 사용 가능** | 13 | 화면 핵심 지표 대부분 |
| **B — 실응답 확인 후 사용** | 8 | 위치는 확정, 의미 대조 필요 |
| **C — 이 API에 없음** | 14 | 전량 외부 소스 또는 파생 필요 |

**핵심 판단**: `nph-aws2_min` 하나로는 [C] 상세 블록이 거의 다 채워지지만,
사용자가 날씨 화면에서 가장 먼저 보는 [B] 대표 블록(날씨 상태·아이콘·체감·최고/최저)은
**단 하나도 채워지지 않는다.** 이 API는 "현재 관측 실측값" 전용이고 "예보·상태 판정"이
없기 때문이다. 단일 API로 화면을 완성하려는 설계는 성립하지 않는다.

---

## 5. 부재 항목(C등급) 대안

지어낸 필드로 채우지 않고, 각 항목이 실제로 어디서 오는지 명시한다.

### 5.1 파생 계산으로 해결 — 추가 API 불필요

| 화면 항목 | 입력 | 방법 |
|---|---|---|
| 체감온도 | `ta`, `ws1`, `hm` | 저온에서는 풍속냉각지수, 고온다습에서는 열지수를 쓴다. **산식 상수는 기상청 공식 문서로 확정할 것** — 이 문서에 상수를 적지 않은 이유는 검증하지 못했기 때문이다. |
| 16방위 풍향 | `wd1` | 이미 구현됨 (`_compass`) |
| 불쾌지수 | `ta`, `hm` | 표준 산식, 출처 명기 후 적용 |
| 강수 유무 배지 | `re` | 구현됨 |

### 5.2 추가 API 필요 — 기상청 계열

| 화면 항목 | 필요한 서비스 | 제공 키 | 상태 |
|---|---|---|---|
| 날씨 상태 / 아이콘 | 단기예보 조회서비스 `getVilageFcst` 의 `SKY` | `KMA_API_KEY` (공공데이터포털) | **미구현** |
| 강수 형태 (비/눈) | 동일 서비스의 `PTY` | 동일 | **미구현** |
| 강수확률 | 동일 서비스의 `POP` | 동일 | **미구현** |
| 시간별 예보 | 초단기예보 `getUltraSrtFcst` (6시간) | 동일 | **미구현** |
| 주간 예보 | 중기예보 조회서비스 | 별도 활용신청 | **미구현** |
| 최고/최저 기온 | 단기예보 `TMX` / `TMN` | `KMA_API_KEY` | **미구현** |
| 기상특보 배너 | `WthrWrnInfoService/getWthrWrnList` | `KMA_API_KEY` | ✅ **본 저장소에 구현됨** (`get_weather_alerts`) |
| 지점명 / 지점 확장 | API Hub `stn_inf.php` | `KMA_APIHUB_KEY` (별도 활용신청) | **미구현** |

> 단기예보 계열은 위·경도가 아니라 격자 `(nx, ny)` 를 받는다. **본 저장소의
> `latlon_to_grid()` 가 이미 기상청 Lambert Conformal Conic 변환을 구현해 두었고**
> 서울(60,127)·부산(98,76)·인천(55,124)·대전(67,100)·광주(58,74)·울산(102,84)·
> 울릉도(127,127) 기준값으로 검증돼 있다. 확장 시 그대로 재사용하면 된다.

### 5.3 기상청 외부 소스

| 화면 항목 | 제공처 |
|---|---|
| 미세먼지 / 초미세먼지 | 한국환경공단 에어코리아 (공공데이터포털) |
| 자외선 지수 | 기상청 생활기상지수 조회서비스 (별도 활용신청) |
| 일출 / 일몰 | 한국천문연구원 출몰시각 정보 (공공데이터포털) |

### 5.4 최고/최저 기온의 현실적 처리

단기예보 `TMX`/`TMN` 도입 전까지의 임시 대안:

1. 매분자료를 주기 폴링해 `ta` 를 로컬 누적, 당일 min/max 산출
2. 단, 서버 기동 이후 구간만 반영되므로 **"관측 시작 이후 기준" 라벨 필수**
3. 라벨 없이 "오늘의 최고기온"으로 표기하면 오정보다 — 금지

---

## 6. 데이터 설계

### 6.1 화면 뷰모델

3장 대응표를 그대로 타입으로 옮긴 것이다. 결측 가능 필드는 전부 nullable.

```
RegionWeather
├─ region_name      : string          ← 클라이언트 STATIONS 역매핑 (C: API 미제공)
├─ station_id       : int             ← stn
├─ observed_at      : ISO8601(+09:00) ← tm
├─ temperature_c    : float | null    ← ta
├─ humidity_pct     : float | null    ← hm
├─ dew_point_c      : float | null    ← td
├─ pressure_hpa     : float | null    ← pa
├─ pressure_sea_hpa : float | null    ← ps   [B]
├─ precip_state     : "있음"|"없음"|"알 수 없음"  ← re
├─ precip_1h_mm     : float | null    ← rn60m
├─ precip_15m_mm    : float | null    ← rn15m  [B]
├─ precip_12h_mm    : float | null    ← rn12h  [B]
├─ precip_day_mm    : float | null    ← rnday  [B]
├─ wind_speed_ms    : float | null    ← ws1
├─ wind_deg         : float | null    ← wd1
├─ wind_dir_16      : string | null   ← wd1 파생
├─ gust_speed_ms    : float | null    ← wss    [B]
├─ feels_like_c     : float | null    ← 파생 (5.1)
└─ sky_state        : null            ← 미대응 (5.2), 필드는 두되 항상 null
```

`sky_state` 처럼 아직 못 채우는 필드는 **자리를 비워 두되 임의값으로 채우지 않는다.**
UI는 `null` 을 "정보 없음"으로 렌더링하고, 단기예보 연동 시 그 자리만 채운다.

### 6.2 파싱 파이프라인

```
① HTTP GET (authKey, stn, tm2, disp=0, help=1)
        ↓
② 본문이 '{' 로 시작? ─ 예 → JSON 오류 객체 → result.message 추출 → ToolError
        │ 아니오
        ↓
③ 줄 단위 순회: 빈 줄 / '#' 시작 줄 skip
        ↓
④ 공백 split → len(parts) >= 18 && parts[1] == str(stn) 인 행 선택
        ↓
⑤ zip(_AWS_FIELDS, parts) → dict
        ↓
⑥ 숫자 필드는 _aws_float(): float 변환 실패 → null, 값 <= -50 → null
        ↓
⑦ 뷰모델 매핑 + 파생값(16방위, 체감온도) 계산
```

**⑥의 -50 임계값이 이 설계의 핵심 방어선이다.** 기상청은 결측을 `-99.9` 로 보내지만
정확 비교(`== -99.9`) 대신 임계값을 쓴 것은 다른 에러 코드까지 포괄하기 위해서다.
`-9.9` 같은 정상 영하 기온은 임계값 위라 보존된다. 이 로직을 약화시키면 화면에
`-99.9℃` 가 노출된다.

### 6.3 관측 지연 재시도

매분자료는 발표까지 수 분의 지연이 있어 현재 시각으로 조회하면 빈 응답이 온다.

```
tm2 = now(KST) - 2분  →  데이터 행 있으면 채택
             실패 ↓
tm2 = now(KST) - 7분  →  데이터 행 있으면 채택
             실패 ↓
             "실황 자료 없음" 오류
```

버퍼값은 `_AWS_RETRY_BUFFERS_MIN = (2, 7)` 로 상수화돼 있다. 화면 단에서는
**요청 시각이 아니라 응답의 `tm` 을 표시해야 한다.** 둘은 최대 7분 어긋난다.

### 6.4 캐시 정책

| 항목 | 값 | 근거 |
|---|---|---|
| TTL | 60초 | 원천이 매분 갱신이라 그보다 짧게 잡으면 낭비 |
| 캐시 키 | `stn` + `tm` | 시각 기준이라 동일 분 내 중복 호출 제거 |
| 동시 요청 | 지점별 단일화(coalescing) | 같은 지역 동시 조회 시 상류 1회만 |

### 6.5 오류 표면

화면이 구분해서 보여줘야 하는 실패 상태다. 전부 다른 메시지를 줘야 한다.

| 상태 | 감지 | 화면 처리 |
|---|---|---|
| 인증키 미설정 | 환경변수 공백 | 설정 안내 (사용자 조치 가능) |
| 인증/활용신청 오류 | 응답이 JSON 오류 객체 | 관리자 문의 |
| 네트워크/타임아웃 | 예외 (timeout 30s) | 재시도 버튼 |
| 지점번호 미지원 | `STATIONS` 미스 | 지원 지역 목록 제시 |
| 실황 자료 없음 | 2회 재시도 후 행 없음 | "일시적으로 관측값 없음" |
| 일부 결측 | 개별 필드 `null` | **전체 실패 아님.** 해당 항목만 "—" 표기 |

마지막 행이 중요하다. 한 센서가 결측이라고 화면 전체를 오류로 떨구면 안 된다.

### 6.6 지역 커버리지 제약

`nph-aws2_min` 은 **위·경도를 받지 않고 지점번호만 받는다.** 현재 자동 해석되는
지역은 8곳이다.

| 지역 | `stn` | 지역 | `stn` |
|---|---|---|---|
| 서울 | 108 | 광주 | 156 |
| 인천 | 112 | 대전 | 133 |
| 부산 | 159 | 울산 | 152 |
| 대구 | 143 | 제주 | 184 |

그 외 지역은 호출자가 `stn` 을 직접 넘겨야 한다. "지역 날씨" 화면에 지역 선택
UI를 둔다면 **이 8곳이 곧 선택 가능 목록의 상한**이다. 확장하려면 5.2의
`stn_inf.php`(별도 활용신청) 로 지점 목록을 받아 채워야 한다.

`kma://locations` 리소스에 지명 91곳의 위·경도와 격자가 들어 있지만, 이는
단기예보 확장을 위한 자산이지 이 API의 지점 커버리지가 아니다. **혼동하면
"지원되는 줄 알았는데 조회가 안 되는" 지역이 생긴다.**

---

## 7. 구현 순서

| 단계 | 작업 | 선행조건 |
|---|---|---|
| 1 | `help=1` 실응답 확보 → B등급 8개를 A로 승격, 헤더 표기 확정 | 네트워크 허용 + authKey |
| 2 | [C] 상세 블록 구현 (A등급 13개) | 1단계 불필요, 즉시 가능 |
| 3 | 체감온도 파생 — 기상청 공식 산식 출처 확보 후 | 산식 검증 |
| 4 | 단기예보 연동 → `SKY`/`PTY`/`POP`/`TMX`/`TMN` → [B] 대표 블록 완성 | 활용신청 |
| 5 | 특보 배너 — 기구현 `get_weather_alerts` 연결 | 없음 |
| 6 | 지점 확장 (`stn_inf.php`) 또는 최근접 지점 탐색 | 활용신청 |
| 7 | 대기질·자외선·일출몰 외부 연동 | 각 기관 신청 |

2단계는 지금 바로 착수 가능하다. 4단계가 사용자 체감상 가장 큰 항목이지만
별도 활용신청이 선행돼야 한다.

---

## 8. 미해결 항목

이 문서가 확정하지 못한 것들이다. 넘겨짚지 않고 남긴다.

1. **B등급 8개 필드의 실제 의미** — `help=1` 범례로만 확정된다.
2. **응답 헤더의 정확한 표기** — `RN-15m` 인지 `RN_15M` 인지 등. 인덱스 파싱이라
   동작에는 무관하나 문서 정확도를 위해 확인 필요.
3. **`re` 필드의 값 도메인** — 코드는 `>=1` 을 감지로 보지만, 0/1 이진인지 강도
   단계가 있는지 미확인.
4. **체감온도 공식 상수** — 기상청 공식 산식 확인 전까지 구현 보류.
5. **화면 스펙** — 2장은 일반적 구성을 가정한 것이다. 실제 디자인 확정 시 2장을
   고치고 3장을 재매핑할 것.
