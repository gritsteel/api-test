# CLAUDE.md

## 프로젝트

지역별 누적 강수량과 농업용수 저수율을 보여주는 정적 페이지다.
농림축산식품 공공데이터 OpenAPI로 받은 2019-01~2026-09 월별 집계 66,263건을 쓴다.
화면 구성과 필드 대응표는 `PLAN.md`에 정의돼 있다.

## 원칙

- 모든 구현은 PLAN.md를 근거로 한다
- PLAN.md에 없는 기능은 만들지 않는다
- 기획이 바뀌면 PLAN.md를 먼저 고치고, 그다음 코드를 고친다

## 데이터

데이터는 `data.json`에서 읽는다.

## 배포

배포는 `build` 폴더만 한다.
