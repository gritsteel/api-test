# CLAUDE.md

## 프로젝트

지역 날씨 화면을 만드는 프로젝트다.
기상청 지상관측 AWS 매분자료(`nph-aws2_min`)의 관측값을 화면 항목에 대응시켜 보여준다.
화면 구성과 API 필드 대응표는 `PLAN.md`에 정의돼 있다.

## 원칙

- 모든 구현은 PLAN.md를 근거로 한다
- PLAN.md에 없는 기능은 만들지 않는다
- 기획이 바뀌면 PLAN.md를 먼저 고치고, 그다음 코드를 고친다

## 데이터

데이터는 `data.json`에서 읽는다.

## 배포

배포는 `build` 폴더만 한다.
